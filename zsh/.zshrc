#
# Runs for interactive shells, and inherits properties from login shell
# Run order: .zshenv, .zprofile, .zpreztorc (called via Prezto inside .zprofile), .zshrc
#

# Nothing that needs to run per interactive shell
# See .zprofile for most configs
